# Rust build artifacts (root + nested workspace/package crates)
/target
target/
/target-prof
/**/*.rs.bk
/Cargo.lock

# Node
node_modules/

# Logs
*.log

.DS_Store
.vs

[Bb]in/
# The rule above is for .NET build output; the runtime crate's binary targets live in src/bin.
!runtime/src/bin/
[Oo]bj/
AppPackages/
dotnet-bridge/publish/
/dotnet-bridge-benchmarks/BenchmarkDotNet.Artifacts
